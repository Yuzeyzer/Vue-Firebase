rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /playlists/{docId} {
      allow read, write: if request.auth != null;
      allow delete, update: if request.auth.uid == resource.dta.userId;
    }
  }
}